# INTENTIONALLY NON-EXECUTABLE TEMPLATE.
#
# Raw checkout-script cron entries cannot establish source provenance and are
# unsupported. Install no lines from this file. A future scheduled job must use
# a separately reviewed, root-owned launcher implementing the exact clean-parent
# `production_entry host-maintenance [--check-only]` bootstrap in PUBLISH.md.
