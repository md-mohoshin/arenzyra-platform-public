export {
  DS_AUTO,
  derivePcobBindingFlags,
  deriveSourceMode,
  exposeCanonicalTelemetryProvider,
  exposeTelemetryProvider,
  normalizeTelemetryProvider,
  resolveCanonicalTelemetryProvider as resolveMatchDataSource,
  resolveTelemetryProviderInput,
  TELEMETRY_PROVIDERS,
  type DerivedSourceMode,
  type ExposedTelemetryProvider,
  type TelemetryProvider,
} from '../../common/match-telemetry-provider.util';
